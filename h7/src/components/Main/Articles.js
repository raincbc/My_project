import React from 'react'
import styled from 'styled-components'

const Articles = ({id, title, titleSrc, src}) => {
  return (
    <Container>
          
    </Container>
  )
}

export default Articles

const Container = styled.div``;